// Package layout orchestrates node positioning for netdraw diagrams.
// In Phase 3, automatic placement is handled by D2's built-in dagre engine.
// layout_overrides.yml stores per-view manual positions that persist across re-renders.
// Position injection into the D2 compiled graph is planned for Phase 6.
package layout
